# Backend requirements for Phase 5 project

# Flask web framework
flask==2.3.3
flask-cors==4.0.0

# WSGI Server
gunicorn==22.0.0

# Database - Updated versions for Python 3.13 compatibility
sqlalchemy==2.0.35
# For PostgreSQL on Windows, you may need to install psycopg2-binary separately
# If you encounter installation issues with psycopg2-binary, try:
# 1. Install from conda-forge: conda install psycopg2
# 2. Or use the pre-compiled wheel: pip install --only-binary=psycopg2-binary psycopg2-binary
# 3. Or use SQLite instead (comes with Python): uncomment the sqlite3 line below
psycopg2-binary==2.9.7  # For PostgreSQL
# sqlite3  # Built into Python, no need to install

alembic==1.13.1

# Environment variables
python-dotenv==1.0.0

# Testing
pytest==8.3.4
pytest-flask==1.3.0

# Development
flask-migrate==4.0.7
flask-sqlalchemy==3.1.1

# Logging
structlog==24.4.0

# Async processing (if needed)
celery==5.4.0
redis==5.2.0

# Serialization
marshmallow==3.23.1

# Authentication
flask-login==0.6.3
bcrypt==4.2.0

# Utilities
python-dateutil==2.9.0.post0
requests==2.32.3